{
  "name": "cinevault",
  "private": true,
  "description": "Static CineVault; on Vercel set TMDB_API_KEY, TMDB_READ_TOKEN, HF_API_TOKEN",
  "scripts": {
    "build": "node scripts/write-secrets.js"
  }
}
